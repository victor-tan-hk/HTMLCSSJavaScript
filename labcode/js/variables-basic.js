// Older form ES5, avoid the use of this
// You will still see this form used in older tutorials and books
var stuff = 5;

// Declare first and then initialize later
let message;
message = "Hello";

let anotherMessage = "JavaScript is cool";

let myName = 'Thor', myAge = 120;

myName = 'Superman';
myAge = 33;

console.log("My name is ", myName);
console.log("My age is ", myAge);


let unknownMessage;
console.log(unknownMessage);

// Error trying to reference a variable that has never been declared
console.log(counter);

// Cannot change a const variable
const workday = 5;
workday = 2;

